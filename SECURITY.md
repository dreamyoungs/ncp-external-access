# 보안 정책

아직 안정 릴리스나 보안 감사가 완료된 프로젝트가 아닙니다. 지속 지원 기간과 보안 대응 시간을 보장하지 않습니다.

## 제보

공개 이슈에 실제 인증서·개인키·credential·계정 식별자 또는 악용 가능한 상세 정보를 올리지 마세요. 저장소 소유자는 공개 전에 GitHub Private vulnerability reporting을 활성화해야 합니다. 활성화 후 저장소 Security 탭의 비공개 제보 기능을 사용하세요. 아직 기능이 보이지 않으면 민감한 내용 없이 비공개 연락 경로를 요청하세요.

## 자동 검사

CI는 push·PR·수동 실행과 매주 월요일에 다음 검사를 실행합니다. workflow가 기본 브랜치에 올라가기 전에는 예약 검사가 실행되지 않습니다.

- `npm audit --audit-level=low`: 개발 의존성을 포함한 lockfile 전체를 검사하고 low 이상 발견 시 실패합니다. Node.js·OpenSSL·OS 자체의 CVE 검사는 포함하지 않습니다.
- `go run golang.org/x/vuln/cmd/govulncheck@v1.8.0 -show verbose ./...`: 실행 중인 Go toolchain의 표준 라이브러리와 모듈을 분석합니다. 도달 가능한 취약점은 실패시키고, 도달성이 확인되지 않은 발견도 상세 출력합니다. 모든 의존성 CVE가 0건이라는 판정과는 다릅니다.

스캐너 실행·DB 조회 실패도 CI 실패로 처리하며 검사를 생략해 통과시키지 않습니다. Go 스캐너 버전은 명시적으로 고정했으므로 유지보수자가 업데이트를 확인해야 합니다. 통과는 해당 DB·toolchain·플랫폼 기준이며 모든 환경의 무취약점을 보장하지 않습니다.

근거: [Go 취약점 관리](https://go.dev/doc/security/vuln/).

### 최초 로컬 검사 — 2026-09-22

- npm lockfile 전체 검사: 발견 0건. TypeScript 빌드·테스트 4개 및 npm package contents 검사 통과.
- 최초 Go 1.26.0 검사: 표준 라이브러리의 호출 경로 관련 16건으로 실패. 이 결과를 예외 처리하지 않고 최소 Go 버전을 1.26.6으로 올렸습니다.
- Go 1.26.6, darwin/arm64, govulncheck v1.8.0 재검사: `No vulnerabilities found.`. race 테스트·vet도 통과했습니다.
- 위는 로컬 결과입니다. GitHub Actions/Linux 실행 결과나 npm 발행 성공을 의미하지 않습니다. Node 런타임 자체·실제 NCP 상호운용 검증은 이번 검사에 포함하지 않았습니다.

## 사용자 책임과 런타임 관리

- 개인키와 반환 credential을 로그에 남기지 않고, 최소 권한 Role과 적절한 Trust Anchor/Profile 정책을 사용하세요.
- 개인키 보관·복호화·폐기, 인증서 갱신, 세션 갱신·캐시와 재시도는 호출자가 담당합니다.
- Node.js·Go·OS와 TLS 신뢰 저장소를 업데이트하세요. 라이브러리가 작거나 외부 런타임 의존성이 없다는 이유로 무취약점을 보장하지 않습니다.
- TLS 검증을 끄거나 전역 HTTP transport/fetch를 신뢰할 수 없는 구현으로 교체하지 마세요.
- 반환된 키는 만료시간과 인증서 수명을 고려해 사용하세요. 로컬 유효기간 검사는 CA 신뢰·폐기·Role 권한 검증을 대신하지 않으며 이 부분은 NCP 서버가 판단합니다.

원문 provider 응답·오류를 공개 오류 메시지에 포함하지 않습니다. 응답 byte buffer 일부는 사용 후 지우지만, JS/Go 문자열·런타임 복사본·키 객체의 완전한 메모리 삭제는 보장하지 않습니다. 합성 테스트는 실제 계정에 접근하지 않습니다.
